expression: map f (map g fx)
steps:
  - code: |
      map f (\c -> g (fx c))
    explanation: "Definition von `map`"
  - code: |
      \c -> f ((\c -> g (fx c)) c)
    explanation: "Definition von `map`"
  - code: |
      \c -> f (g (fx c))
    explanation: "Anwendung der Lambda-Funktion"
  - code: |
      map (\x -> f (g x)) fx
    explanation: "Definition von `map`"
